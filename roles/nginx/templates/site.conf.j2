server {
        listen   80;
        server_name  {{ SERVER_NAME }};
        root   {{ FILE_PATH }};
        location / {
                index.html;
                autoindex off;    #enable listing of directory index
        }
}
